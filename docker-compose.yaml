version: '3.8'
services:
  playwright-tests:
    image: playwright-tests
    build: 
      context: .
      dockerfile: Dockerfile
    command: npm run uiComponents-chrome  
    volumes:
      - ./playwright-report/:/app/playwright-report/
      - ./test-results/:/app/test-results/
